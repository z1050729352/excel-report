# 烟草订货管理系统 - 启动指南

## 📋 前置要求

- **Node.js**: >= 22.0.0
- **npm**: >= 9.0.0  
- **Docker**: 用于运行 MySQL 数据库

## 🚀 快速启动

### 1️⃣ 启动数据库

```bash
# 启动 MySQL 容器
docker-compose up -d

# 查看容器状态
docker ps

# 查看数据库日志（可选）
docker logs tobacco-mysql
```

**数据库信息：**
- 主机: `localhost`
- 端口: `3307` (映射到容器内的 3306)
- 数据库: `tobacco_order`
- 用户名: `tobacco`
- 密码: `tobacco123`
- Root 密码: `root123456`

**测试连接：**
```bash
# 使用 MySQL 客户端连接
mysql -h 127.0.0.1 -P 3307 -u tobacco -ptobacco123 tobacco_order

# 或使用 Docker 进入容器
docker exec -it tobacco-mysql mysql -u tobacco -ptobacco123 tobacco_order
```

### 2️⃣ 启动后端服务

```bash
# 进入后端目录
cd backend

# 安装依赖（首次运行）
npm install

# 启动开发服务
npm run dev

# 或生产模式
npm start
```

**后端服务信息：**
- 地址: `http://localhost:3001`
- API 前缀: `/api`
- 健康检查: `http://localhost:3001/api/health`

**默认管理员账号：**
- 用户名: `admin`
- 密码: `admin123`

### 3️⃣ 启动前端服务

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务
npm run dev

# 或构建生产版本
npm run build
```

**前端服务信息：**
- 商户端: `http://localhost:3000/`
- 管理端: `http://localhost:3000/admin.html`

## 📚 API 接口文档

### 认证相关
- `POST /api/auth/login` - 管理员登录
- `GET /api/auth/check` - 检查登录状态

### 商户端接口 (C端)
- `GET /api/merchant/:licenseNo` - 查询商户信息
- `POST /api/merchant/plan` - 生成订货方案

### 管理端接口 (B端)
- `GET /api/admin/merchants` - 商户列表（支持分页和搜索）
- `POST /api/admin/import/merchants` - 导入商户信息表
- `POST /api/admin/import/products` - 导入货源投放策略
- `POST /api/admin/import/changes` - 导入信息更变表
- `GET /api/admin/changes` - 获取更变列表
- `POST /api/admin/changes/apply` - 应用更变

## 🔧 常见问题

### Q: Node 版本检查失败？
**A:** 项目要求 Node.js >= 22.0.0

```bash
# 查看当前版本
node -v

# 使用 nvm 切换版本
nvm install 22
nvm use 22
```

### Q: Docker 启动失败？
**A:** 确保 Docker 服务已启动，端口 3307 未被占用

```bash
# 检查端口占用
lsof -i :3307

# 停止并重新启动
docker-compose down
docker-compose up -d
```

### Q: 数据库连接失败？
**A:** 检查以下配置

1. 确认 Docker 容器运行中
2. 检查 `backend/.env` 配置是否正确
3. 端口是否为 3307

### Q: 前端请求 404？
**A:** 确保后端服务已启动在 3001 端口，Vite 会代理 `/api` 到后端

### Q: 如何重置数据库？
**A:** 
```bash
# 停止并删除容器和数据卷
docker-compose down -v

# 重新启动（会自动执行初始化脚本）
docker-compose up -d
```

## 📂 项目结构

```
excel-report/
├── backend/              # 后端服务 (Node.js + Express)
│   ├── src/
│   │   ├── app.js       # 主应用入口
│   │   ├── config/      # 配置文件
│   │   ├── middleware/  # 中间件（认证、上传等）
│   │   ├── models/      # 数据库模型
│   │   ├── routes/      # 路由定义
│   │   └── services/    # 业务逻辑（Excel解析、优化算法）
│   ├── .env             # 环境变量
│   └── package.json
│
├── frontend/             # 前端服务 (Vue 3 + Vite)
│   ├── src/
│   │   ├── admin/       # 管理端（B端）
│   │   │   ├── views/   # 页面组件
│   │   │   ├── router/  # 路由配置
│   │   │   └── layouts/ # 布局组件
│   │   ├── client/      # 商户端（C端）
│   │   │   ├── views/   # 页面组件
│   │   │   ├── router/  # 路由配置
│   │   │   ├── stores/  # Pinia状态管理
│   │   │   └── layouts/ # 布局组件
│   │   └── shared/      # 共享资源
│   │       ├── api/     # API请求封装
│   │       └── utils/   # 工具函数
│   ├── index.html       # 商户端入口
│   ├── admin.html       # 管理端入口
│   ├── vite.config.js   # Vite配置
│   └── package.json
│
├── database/
│   └── init.sql         # 数据库初始化脚本
├── docker-compose.yml   # Docker配置
├── check-node-version.js # Node版本检查
└── .nvmrc               # Node版本指定

```

## 🎯 开发流程建议

1. **首次启动顺序**：
   ```bash
   # 1. 启动数据库
   docker-compose up -d
   
   # 2. 启动后端（新终端）
   cd backend && npm run dev
   
   # 3. 启动前端（新终端）
   cd frontend && npm run dev
   ```

2. **每次新增功能**：
   - 后端：修改代码后自动重启（使用 `--watch`）
   - 前端：修改代码后自动热更新
   - 在浏览器中实时查看效果

3. **调试技巧**：
   - 后端日志：终端查看请求日志
   - 前端调试：浏览器 DevTools
   - 数据库查询：使用 MySQL 客户端或 TablePlus

## 📦 生产部署

### 构建前端
```bash
cd frontend
npm run build
# 输出到 dist/ 目录
```

### 后端部署
```bash
cd backend
# 使用 PM2 或直接运行
npm start
```

### 服务器配置建议
- 使用 Nginx 反向代理
- 前端静态文件部署到 CDN
- 后端 API 使用 PM2 守护进程
- MySQL 使用云数据库或独立部署

## 🔐 安全注意事项

1. **生产环境必须修改**：
   - `.env` 中的数据库密码
   - JWT_SECRET 密钥
   - 管理员默认密码

2. **建议配置**：
   - 启用 HTTPS
   - 配置 CORS 白名单
   - 添加请求频率限制
   - 启用数据库备份

## 📞 技术支持

如有问题，请检查：
1. Node 版本是否正确（>= 22）
2. Docker 是否正常运行
3. 端口是否被占用（3000, 3001, 3307）
4. `.env` 配置是否正确
