# 项目架构对比

## 📋 架构演进概览

### 阶段 1：Electron 桌面应用（已废弃）
```
electron-app/
├── src/
│   ├── main/          # Electron 主进程
│   ├── preload/       # 预加载脚本
│   └── renderer/      # Vue 渲染进程
├── electron.vite.config.mjs
└── package.json       # Electron 依赖
```

**特点**：
- ❌ 仅支持桌面端（macOS/Windows）
- ❌ SQLite 本地数据库
- ❌ 无法多用户协作
- ❌ 部署困难（需要打包成安装包）
- ❌ 更新困难（需要重新下载安装）

---

### 阶段 2：前后端分离 Web 应用（当前架构）✅

```
tobacco-order-system/
├── backend/                    # 后端服务
│   ├── src/
│   │   ├── app.js             # Express 应用
│   │   ├── routes/            # API 路由
│   │   ├── services/          # 业务逻辑
│   │   └── middleware/        # 中间件
│   └── package.json
│
├── frontend/                   # 前端应用
│   ├── src/
│   │   ├── admin/             # 管理端（PC优先）
│   │   ├── client/            # 商户端（H5优先）
│   │   └── shared/            # 共享代码
│   ├── admin.html             # 管理端入口
│   ├── index.html             # 商户端入口
│   └── package.json
│
├── data/                       # 数据文件
├── database/                   # SQL 脚本
├── docker-compose.yml          # 容器编排
└── package.json               # Monorepo 配置
```

**特点**：
- ✅ 支持 PC + H5 双端访问
- ✅ MySQL 数据库（支持多用户并发）
- ✅ 前后端分离（易于扩展和维护）
- ✅ Docker 容器化（一键部署）
- ✅ 热更新（无需重新安装）
- ✅ 管理端与商户端分离（不同角色不同界面）

---

## 🔄 技术栈对比

| 维度 | Electron 桌面端 | Web 前后端分离 |
|------|----------------|----------------|
| **前端框架** | Vue 3 | Vue 3 |
| **后端** | Electron Main Process | Node.js + Express |
| **数据库** | SQLite (本地文件) | MySQL (服务器) |
| **UI 组件库** | 无 | Element Plus |
| **状态管理** | 无 | Pinia |
| **路由** | 无 | Vue Router |
| **打包工具** | electron-vite | Vite |
| **部署方式** | 打包成 .dmg/.exe | 部署到服务器 |
| **访问方式** | 下载安装 | 浏览器访问 |
| **更新方式** | 重新下载安装 | 刷新浏览器 |
| **多用户** | ❌ 不支持 | ✅ 支持 |
| **移动端** | ❌ 不支持 | ✅ 支持 H5 |

---

## 📱 端适配策略

### 管理端（Admin）
- **主要用户**：管理员
- **主要场景**：PC 办公
- **UI 策略**：
  - PC：左侧菜单 + 右侧内容区域
  - H5：顶部菜单 + 主内容区域（可折叠菜单）
  - 组件库：Element Plus（响应式布局）

### 商户端（Client）
- **主要用户**：商户
- **主要场景**：移动端查询订货
- **UI 策略**：
  - H5 优先设计（手机竖屏）
  - PC 访问时居中展示，最大宽度限制
  - 组件库：Element Plus 或 Vant（待定）

---

## 🚀 部署架构

### 开发环境
```
开发机器
├── MySQL (Docker)  :3307
├── Backend         :3001
└── Frontend        :3000 (Vite Dev Server)
```

### 生产环境（规划）
```
阿里云服务器
├── Nginx          :80/443
│   ├── / → 商户端静态文件
│   ├── /admin → 管理端静态文件
│   └── /api → 后端服务 (反向代理)
├── Node.js Backend :3001
└── MySQL          :3306
```

---

## 🎯 迁移收益

### 技术收益
1. **更好的可维护性**：前后端职责分离，代码结构清晰
2. **更强的扩展性**：可以独立扩展前端或后端
3. **更容易的部署**：Docker 一键部署，无需复杂的桌面应用打包
4. **更快的迭代**：热更新，无需重新安装

### 业务收益
1. **多端访问**：PC、手机、平板都能访问
2. **实时协作**：多个管理员可以同时操作
3. **数据集中**：所有数据存储在服务器，便于备份和管理
4. **远程访问**：只要有网络，随时随地可以访问

### 用户体验收益
1. **商户端**：手机扫码即可访问，无需安装 APP
2. **管理端**：浏览器访问，无需安装客户端
3. **更新透明**：服务端更新后，用户刷新浏览器即可获得新功能

---

## 📊 代码量对比

### 删除的代码（Electron 相关）
- `src/main/` - Electron 主进程：~500 行
- `src/preload/` - 预加载脚本：~50 行
- `src/renderer/` - 渲染进程（旧版）：~1000 行
- 配置文件：~200 行
- **总计删除：~1750 行**

### 新增的代码（Web 应用）
- `backend/` - 后端服务：~2000 行
- `frontend/src/admin/` - 管理端：~1500 行
- `frontend/src/client/` - 商户端：~1200 行
- `frontend/src/shared/` - 共享模块：~300 行
- **总计新增：~5000 行**

**净增加：~3250 行代码**（但功能更完整、架构更清晰）

---

## ✅ 清理完成检查清单

- [x] 删除 `src/` 目录
- [x] 删除 `out/` 目录
- [x] 删除 `electron.vite.config.mjs`
- [x] 删除 `vite.web.config.mjs`
- [x] 清理根目录 `node_modules/`
- [x] 更新根目录 `package.json`
- [x] 更新 `.gitignore`
- [x] 更新 `README.md`
- [x] 移动 Excel 文件到 `data/`
- [x] 验证服务正常运行
- [x] 创建清理文档

---

**迁移完成时间**：2026年8月15日  
**当前状态**：✅ 架构迁移完成，服务运行正常  
**下一步**：功能测试和 UI 完善
