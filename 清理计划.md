# 桌面端遗留文件清理计划

## 📦 需要清理的文件和目录

### 1. Electron 相关目录（整个删除）
- `src/` - 旧的 Electron 项目源码目录
  - `src/main/` - Electron 主进程
  - `src/preload/` - Electron 预加载脚本
  - `src/renderer/` - Electron 渲染进程（旧前端）
  - `src/server/` - Electron 内嵌的服务器
  - `src/core/` - 核心业务逻辑（已迁移到 backend/src/services/）

### 2. Electron 配置文件
- `electron.vite.config.mjs` - Electron Vite 配置

### 3. 根目录 package.json 中的 Electron 依赖
需要删除的依赖：
- `@electron-toolkit/preload`
- `@electron-toolkit/utils`
- `electron`
- `electron-builder`
- `electron-vite`

需要删除的脚本命令：
- `start` (electron-vite preview)
- `dev` (electron-vite dev)
- `build` (electron-vite build)
- `rebuild:native:electron`
- `postinstall` (electron-builder)
- `build:mac`
- `build:win`

### 4. 保留的重要文件
⚠️ 注意：这些 Excel 文件需要移动到新位置，不要删除！
- `src/货源投放策略7.23.xls` → 移动到 `data/` 目录
- `src/烟草进价零售价毛利表.xlsx` → 移动到 `data/` 目录

## ✅ 清理后的项目结构

```
excel-report/
├── backend/              # ✅ 新的后端服务
├── frontend/             # ✅ 新的前端应用
├── database/             # ✅ 数据库初始化脚本
├── data/                 # ✅ 数据文件存储
│   ├── reports/         # 生成的报告
│   ├── tobacco.db       # SQLite（旧，可保留备份）
│   ├── 货源投放策略7.23.xls
│   └── 烟草进价零售价毛利表.xlsx
├── docker-compose.yml    # ✅ Docker 配置
├── check-node-version.js # ✅ 版本检查
├── .nvmrc               # ✅ Node 版本指定
├── README.md            # ✅ 项目说明
├── START.md             # ✅ 启动说明
└── 启动指南.md          # ✅ 详细启动指南
```

## 🎯 清理步骤

1. 移动重要的 Excel 文件到 data/ 目录
2. 删除 src/ 目录
3. 删除 electron.vite.config.mjs
4. 清理根目录的 package.json 和 package-lock.json
5. 重新安装依赖（如果需要）

## ⚠️ 注意事项

- 清理前请确认已备份重要数据
- Excel 文件包含业务数据，必须保留
- 建议先提交 Git 再进行清理，方便回滚
